name: CI

on:
  push:
    branches:
      - '*'

jobs:
  pre-commit-check:
    runs-on: ubuntu-latest
    container:
      image: python:3.11
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Install pre-commit
        run: pip install pre-commit
      - name: Run pre-commit checks
        run: pre-commit run --all-files --show-diff-on-failure
        working-directory: backend  # Replace 'backend' with your project directory name

  backend-compile:
    runs-on: ubuntu-latest
    container:
      image: node:21-alpine3.18
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Install dependencies
        run: |
          yarn install
        working-directory: backend  # Replace 'backend' with your project directory name
      - name: Compile TypeScript
        run: |
          yarn tsc
        working-directory: backend  # Replace 'backend' with your project directory name

  frontend-compile:
    runs-on: ubuntu-latest
    container:
      image: node:21-alpine3.18
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Install dependencies
        run: |
          yarn install
        working-directory: frontend  # Replace 'frontend' with your project directory name
      - name: Compile TypeScript
        run: |
          yarn tsc
        working-directory: frontend  # Replace 'frontend' with your project directory name

  frontend-test:
    runs-on: ubuntu-latest
    container:
      image: node:21-alpine3.18
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Install dependencies
        run: |
          yarn install
        working-directory: frontend  # Replace 'frontend' with your project directory name
      - name: Run tests
        run: |
          yarn test
        working-directory: frontend  # Replace 'frontend' with your project directory name
